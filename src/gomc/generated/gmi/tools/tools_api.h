// Generated by gmicompile - DO NOT EDIT
#ifndef TOOLS_API_H
#define TOOLS_API_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

// --- Types ---

typedef struct tools_tool_entry {
    int32_t toolno;
    int32_t pocketno;
    double x_offset;
    double y_offset;
    double z_offset;
    double a_offset;
    double b_offset;
    double c_offset;
    double u_offset;
    double v_offset;
    double w_offset;
    double diameter;
    double frontangle;
    double backangle;
    int32_t orientation;
    const char * comment;
} tools_tool_entry_t;

typedef struct tools_put_tool_result {
    bool ok;
    int32_t index;
} tools_put_tool_result_t;

typedef struct tools_cmd_result {
    const char * ok;
} tools_cmd_result_t;

// --- Callback Typedefs ---

typedef struct {
    tools_tool_entry_t *data;
    size_t len;
} tools_list_tools_result_t;

typedef tools_list_tools_result_t (*tools_list_tools_fn)(
    void *ctx
);

typedef tools_tool_entry_t (*tools_get_tool_fn)(
    void *ctx,
    int32_t toolno
);

typedef tools_put_tool_result_t (*tools_put_tool_fn)(
    void *ctx,
    int32_t toolno,
    const tools_tool_entry_t *entry
);

typedef tools_cmd_result_t (*tools_delete_tool_fn)(
    void *ctx,
    int32_t toolno
);

typedef tools_cmd_result_t (*tools_reload_tools_fn)(
    void *ctx
);

// --- Callbacks Struct ---

typedef struct tools_callbacks {
    void *ctx;
    tools_list_tools_fn list_tools;
    tools_get_tool_fn get_tool;
    tools_put_tool_fn put_tool;
    tools_delete_tool_fn delete_tool;
    tools_reload_tools_fn reload_tools;
} tools_callbacks_t;

// Convenience macro for modcompile gmi_provide integration.
// Maps gmi_tools_<name>() user functions to callback struct fields.
#define GMI_TOOLS_CALLBACKS { \
    .list_tools = gmi_tools_list_tools, \
    .get_tool = gmi_tools_get_tool, \
    .put_tool = gmi_tools_put_tool, \
    .delete_tool = gmi_tools_delete_tool, \
    .reload_tools = gmi_tools_reload_tools \
}

// --- Registration & Lookup ---
//
// These static inline wrappers call through the gomc_api_t callback
// table provided in cmod_env_t.  No extern symbols -- the C plugin
// has zero undefined references to the Go launcher.
// Skipped when included from the cgo dispatcher (types only).

#ifndef TOOLS_API_CGO

#include "gomc_api.h"

static inline int tools_api_register(
    const gomc_api_t *api,
    const char *instance_name,
    const tools_callbacks_t *callbacks)
{
    return api->register_api(api->ctx, "tools", 1,
                             instance_name, callbacks);
}

static inline const tools_callbacks_t *tools_api_get(
    const gomc_api_t *api,
    const char *instance_name)
{
    return (const tools_callbacks_t *)api->get_api(
        api->ctx, "tools", 1, instance_name);
}

#endif // TOOLS_API_CGO

#ifdef __cplusplus
}
#endif

#endif // TOOLS_API_H
