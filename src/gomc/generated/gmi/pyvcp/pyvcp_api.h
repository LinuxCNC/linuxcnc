// Generated by gmicompile - DO NOT EDIT
#ifndef PYVCP_API_H
#define PYVCP_API_H

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

// --- Enums ---

typedef enum {
    PYVCP_BIT = 1,
    PYVCP_FLOAT = 2,
    PYVCP_S32 = 3,
    PYVCP_U32 = 4
} pyvcp_hal_type_t;

typedef enum {
    PYVCP_IN = 16,
    PYVCP_OUT = 32
} pyvcp_pin_dir_t;

// --- Types ---

typedef struct pyvcp_pin_def {
    const char * name;
    pyvcp_hal_type_t hal_type;
    pyvcp_pin_dir_t dir;
} pyvcp_pin_def_t;

typedef struct pyvcp_pin_value {
    const char * name;
    const char * value;
} pyvcp_pin_value_t;

typedef struct pyvcp_panel_info {
    const char * name;
    const char * xml;
    pyvcp_pin_def_t * pins;
    size_t pins_len;
} pyvcp_panel_info_t;

// --- Callback Typedefs ---

typedef struct {
    const char * *data;
    size_t len;
} pyvcp_list_panels_result_t;

typedef struct {
    pyvcp_pin_value_t *data;
    size_t len;
} pyvcp_watch_pins_result_t;

typedef pyvcp_list_panels_result_t (*pyvcp_list_panels_fn)(
    void *ctx
);

typedef pyvcp_panel_info_t (*pyvcp_get_panel_fn)(
    void *ctx,
    const char * name
);

typedef pyvcp_watch_pins_result_t (*pyvcp_watch_pins_fn)(
    void *ctx,
    const char * panel
);

typedef bool (*pyvcp_set_pin_fn)(
    void *ctx,
    const char * panel,
    const char * name,
    const char * value
);

// --- Callbacks Struct ---

typedef struct pyvcp_callbacks {
    void *ctx;
    pyvcp_list_panels_fn list_panels;
    pyvcp_get_panel_fn get_panel;
    pyvcp_watch_pins_fn watch_pins;
    pyvcp_set_pin_fn set_pin;
} pyvcp_callbacks_t;

// Convenience macro for modcompile gmi_provide integration.
// Maps gmi_pyvcp_<name>() user functions to callback struct fields.
#define GMI_PYVCP_CALLBACKS { \
    .list_panels = gmi_pyvcp_list_panels, \
    .get_panel = gmi_pyvcp_get_panel, \
    .watch_pins = gmi_pyvcp_watch_pins, \
    .set_pin = gmi_pyvcp_set_pin \
}

// --- Registration & Lookup ---
//
// These static inline wrappers call through the gomc_api_t callback
// table provided in cmod_env_t.  No extern symbols -- the C plugin
// has zero undefined references to the Go launcher.
// Skipped when included from the cgo dispatcher (types only).

#ifndef PYVCP_API_CGO

#include "gomc_api.h"

static inline int pyvcp_api_register(
    const gomc_api_t *api,
    const char *instance_name,
    const pyvcp_callbacks_t *callbacks)
{
    return api->register_api(api->ctx, "pyvcp", 1,
                             instance_name, callbacks);
}

static inline const pyvcp_callbacks_t *pyvcp_api_get(
    const gomc_api_t *api,
    const char *instance_name)
{
    return (const pyvcp_callbacks_t *)api->get_api(
        api->ctx, "pyvcp", 1, instance_name);
}

#endif // PYVCP_API_CGO

#ifdef __cplusplus
}
#endif

#endif // PYVCP_API_H
