component sample_hold "Sample and Hold";
pin in signed in;
pin in bit hold;
pin out signed out;
option period no;
function _ nofp;
see_also "*tristate*(9)";
license "GPL";
author "Stephen Wille Padnos";
;;
FUNCTION(_) { 
	if (!hold) {
		out = in;
	}
}
