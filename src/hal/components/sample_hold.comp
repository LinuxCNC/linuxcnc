component sample_hold "Sample and Hold";
pin in sint in;
pin in bool hold;
pin out sint out;
option period no;
function _;
see_also "*tristate*(9)";
license "GPL";
author "Stephen Wille Padnos";
;;
FUNCTION(_) { 
	if (!hold) {
		out_set(in);
	}
}
