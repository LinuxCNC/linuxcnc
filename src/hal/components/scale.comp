component scale;
pin in float in;
pin in float gain;
pin in float offset;
pin out float out "out = in * gain + offset";
function _;
;;
MODULE_LICENSE("GPL");
FUNCTION(_) {
    out = in * gain + offset;
}
