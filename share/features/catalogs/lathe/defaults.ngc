(   Lathe deafults )

; You can customize this file to suit your needs but it will be
; overwritten when updates are available. To prevent this overwriting,
; save it under the name 'defaults_custom'.ngc
; This file if it exists will be loaded, if not it will load the
; standard 'defaults.ngc'
; Best is to create a default template by loading the features you need,
; setting your values and saving it.
; Default Template is loaded every time you want to create a new job.
; If not needed anymore delete 'catalogs/lathe/def_template.xml'
;
; You can also customize menu the same way and rename it 'menu_custom.xml'

	G18	

	#<_global_thread_j> = 1
	#<_global_thread_r> = 1.7
	#<_global_thread_h> = 1
	#<_global_thread_q> = 29.5
	#<_global_thread_e> = 2
	#<_global_thread_l> = 3

	#<_global_surface> = 40
	#<_global_surface_id> = 0
	#<_global_depth> = 10
	#<_global_depth_step> = 1
	#<_global_rappid> = 70
	#<_global_rappid_id> = 0
	#<_global_rappid_z> = 50
	#<_global_final> = 1
	#<_global_final_num> = 2
	#<_global_final_feed> = 1000
	#<_global_feed> = 10000

	#<_global_spindle> =	1000
	#<_global_final_spindle> = 1000
( / Lathe deafults )
