#INCLUDE ../common-config/mill_XYZA_base.ini

# Choose the test program to run as the "display" which 
# runs a headless instance, to test everything downstream of the GUI
[DISPLAY]
DISPLAY = ./headless_batch_test.py

