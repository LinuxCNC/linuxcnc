#!/bin/bash
D=$(dirname "$1"); LOG="$D/server.log"
set -e
grep -q "test_interp_ext: o<test_oword> registered" "$LOG"
grep -q "test_interp_ext: oword 'test_oword' called (n_args=0), returning 42" "$LOG"
echo "interp_ext register_oword: registered a C O-word and it was dispatched from g-code"
