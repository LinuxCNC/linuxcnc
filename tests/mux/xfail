Reworked to the correct gomc multi-instance usage: instead of the classic single
loadrt mux_generic config="b2b,b2f,...,f5f" (which gomc rightly rejects — that comma
list was wrong usage, not a bug), load each mux separately:
load mux_generic <mux-gen.NN> config=<code>. All 18 type-converting muxes build and
produce the CORRECT output values (rework validated).
xfails only on a streaming-TIMING difference shared with tests/multiclick: gomc's
live streamer/sampler feed does not reproduce the classic one-data-line-per-thread-
cycle temporal pattern, so held inputs yield a different number of repeated sample
rows (values match; row multiplicity differs; mux-gen.16 also has debounce-us timing).
Passes once the streamer advance is paced one-line-per-thread-cycle.
