; G28.2 Pn: home a single joint by its 0-based joint number (matching
; [JOINT_n] INI section numbering), instead of all joints.
; Bare G28.2 (no P) is unaffected and still homes every joint.
g28.2 p1       ; home joint 1 only
g28.2          ; home all joints (unaffected by P support)
m2
