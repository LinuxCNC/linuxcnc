#
# @configure_input@ 
# on @DATE@
#
# 
# this file is used for running installed
# it should be in /etc/emc2

# make some useful paths
PREFIX=@prefix@

EMC2_BIN_DIR=$PREFIX/bin
EMC2_TCL_DIR=$PREFIX/share/emc2/tcl
EMC2_SCRIPT_DIR=$PREFIX/bin
EMC2_RTLIB_DIR=@MODULE_DIR@

EMC2_CONFIG_DIR=/etc/emc2


INIFILE=

INIVAR=$EMC2_BIN_DIR/inivar

HALCMD=$EMC2_BIN_DIR/halcmd

REALTIME=/etc/init.d/realtime

# extract info from the ini file that we will need later
# get name of RS274NGC parameter file

KILL_TASK=
KILL_TIMEOUT=10
LOCKFILE=/tmp/emc.lock
