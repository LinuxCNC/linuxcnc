o<prepare> sub
(debug, selected_tool=#<tool> selected_pocket=#<pocket>)

#<pocket> = #<_hal[iocontrol.0.tool-prep-pocket]>
(debug, selected_pocket=#<pocket>)

G4 P3

O102 if [#<pocket> GT 10]
        (debug, tool #<tool> not in changer)
O102     return [-1]
O102 endif

M64 P0


; returning a positive value to commit:
o<prepare> endsub [1]
m2
