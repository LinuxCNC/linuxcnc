corexy examples:

corexy_by_kins.ini 
  Demonstrates corexykins kinematics
  (Note: using corexykins kinematics
   on real hardware requires home
   switches triggered by motor
   positions)

corexy_by_hal.ini
  Demonstrates use of trivkins with
  corexy kinematics implemented by a
  hal component
  (Notes: this method supports
  home switches aligned with x,y
  coordinates)

Notes:
 1) The configurations share common ini
    file settings from corey_base.inc
 2) Home switches are simulated
 3) stepgens are configured but no
    connections to hardware are 
    included.


Ejemplos corexy:

corexy_by_kins.ini
   Demuestra la cinemática corexykins
   (Nota: el uso de la cinemática
    corexykins en hardware real 
    requiere interruptores home
    activados por las posiciones
    del motor)

corexy_by_hal.ini
   Demuestra el uso de trivkins con 
   cinemática corexy implementada
   por un componente hal (Notas:
   este método admite interruptores
   home alineados con x,y)

Notas:
  1) Las configuraciones comparten 
     archivos ini comunes de corey_base.inc
  2) Los interruptores home son simulados
  3) los stepgens están configurados 
     pero no se incluyen conexiones 
     al hardware.
