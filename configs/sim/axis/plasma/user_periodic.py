# CUSTOM PERIODIC CODE, RUNS ONCE EVERY CYCLE
# see the file customizing.html for some tips

# example to create a file with all the widget names if a hal pin is not zero
#if comp['my-halbit']:
#    widgets = get_all_children('.')
#    with open('./widgets.txt', 'w') as outFile:
#        for widget in sorted(widgets):
#            outFile.write(f'{widget}\n')
