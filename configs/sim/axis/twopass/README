TWOPASS demo

El procesamiento TWOPASS de los archivos hal permite múltiples especificaciones para las líneas loadrt de componentes.

Para habilitar el procesamiento de dos pasos:

[HAL]
TWOPASS = on, verbose


Para la depuración, use:
TWOPASS = on,verbose,nodelete

La opción detallada aumenta la salida a stdout.
La opción nodelete evita la eliminación de archivos temporales.
