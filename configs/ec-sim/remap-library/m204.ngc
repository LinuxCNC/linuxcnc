(Complete procedure to set the x-axis amplifier mode pins from CSP to HOMING)
(M201 => M203 => M200)

o<m204> sub
;(debug,executing m204.ngc file)
(expose the remap level)
;(debug, m204.ngc; call_level= #<_call_level> remap_level=#<_remap_level>)


(Turn OFF the enable pin)
M65 P#<_ini[MOTION_OUT]X_ENABLE>

(Wait for the brake to engage)
G04 P#<_ini[DWELL]X_BRAKE_DELAY>


(HOMING 0x06)
M65 P#<_ini[MOTION_OUT]XMP_0>
M64 P#<_ini[MOTION_OUT]XMP_1>
M64 P#<_ini[MOTION_OUT]XMP_2>
M65 P#<_ini[MOTION_OUT]XMP_3>
M65 P#<_ini[MOTION_OUT]XMP_4>
M65 P#<_ini[MOTION_OUT]XMP_5>
M65 P#<_ini[MOTION_OUT]XMP_6>
M65 P#<_ini[MOTION_OUT]XMP_7>

(Turn ON the enable pin)
M64 P#<_ini[MOTION_OUT]X_ENABLE>

(Wait for the brake to disengage)
G04 P#<_ini[DWELL]X_BRAKE_DELAY>

o<m204> endsub 
M2
