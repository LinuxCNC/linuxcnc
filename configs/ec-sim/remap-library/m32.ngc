(M32 Tailbody REV)
(Step 1: Tailsleeve REV)
(Step 2: Tailbody REV)
(Note: M31/M32 procedures only start the classicladder routine and wait)
(for classicladder to signal when the routine is finished.)




o<m32> sub
(debug,executing M32 ngc file)
(expose the remap level)
(debug, M32; call_level= #<_call_level> remap_level=#<_remap_level>)
(debug, Tailsleeve Reverse)
M64 P#<_ini[MOTION_OUT]M32_STEP1>
M66 P#<_ini[MOTION_IN]M32_STEP1_DONE> L3 Q15
M65 P#<_ini[MOTION_OUT]M32_STEP1>

(debug, Tailstock Reverse)
M64 P#<_ini[MOTION_OUT]M32_STEP2>
M66 P#<_ini[MOTION_IN]M32_STEP2_DONE> L3 Q15
M65 P#<_ini[MOTION_OUT]M32_STEP2>
(if the procedure times out, the rest of the code will not be executed)
(we need to clean up in the epilog and using on_abort.ngc to reset the state)

o<m32> endsub
M2
